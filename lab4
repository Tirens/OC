#!/bin/bash
while true
do
echo "--------------------------------------------"
echo "Это меню, не стоит переживать"
echo "--------------------------------------------"
echo "1) Общая информация"
echo "2) Математическое вычисление"
echo "3) Удаление файлов заданного расширения в заданном каталоге"
echo "4) Установка времени и даты"
echo "5) Выход"
read i
echo ""
case $i in
"1")	echo "-----------------------------------"
	echo "Это общая информация"
	echo "-----------------------------------"
	echo ""
	echo "Гайшун Иван Александрович ИТИ-12"
	echo " ";
	echo " В начале выполнения практической работы была получена информация о командах. Так с помощью 
команды echo стало возможно выводить текст на экран консоли. Общий цикл сделан с помощью команды
while...do...done, внутри которой разместилось меню с выбором пункта, ввод которого осуществляется с
помощью команды read. Выполнение пунктов сделано в виде команды выбора case."
	echo " "
	echo "1) Математическое вычисление сделано с вводом данных read и высчисление, с помощью двойных
скобок, по этапам с занесением результатов в переменные. Вывод значений в формулы и отдельный вывод
результата с использованием echo и printf (для записи в одну строку)."
	echo " "
	echo "2) Удаление файлов заданного расширения проводилось с помощью команды find для массового 
удаления. Введена система подтверждения удаления файлов через case."
	echo " "
	echo "3) Установка даты и времени проводилось с помощью занесения даты и времени в переменные day и 
time. Введение даты и времени в date с соответствующими значениями. Вывод даты - date."
	echo " "
	echo "4) Вывод осуществлен с помощью команды прерывания цикла - break."
	echo " ";;

"2")	echo "x=(№Компьютера + №Вариант) * Возраст"
	echo "Введите номер компьютера: "
	read pc
	echo "Введите свой вариант: "
	read variant
	echo "Введите свой возраст: "
	read age
	resultat=$(( $pc + $variant ))
	Resultat=$(( $resultat * $age ))	
	echo "( "$pc" + "$variant" ) * "$age" = $Resultat
	printf "Полученный результат: "
	printf $Resultat
	echo " ";
	echo " ";;

"3") 	echo "Вы решили удалить файлы одного расширения? Отличная идея, погнали удалять зло с компьютера"
	echo "Введите путь каталога: "
	read p
	echo "Введите расширение файлов (Без точки): "
	read format
	echo "Вы действительно хотите удалить эти файлы?"
	find $p -name "*.$format" -type f
	echo "[Yes]/[No] Y/N"
	read question
	case $question in
	"Y" | 'y' | 'Yes' | "YES")
		find $p -name "*.$format" -type f -delete
		echo "Успех! Были удалены все файлы с расширением "$format" по пути: "$p""
		echo " ";;
	"N" | "n" | "No" | "NO")
		echo "Операция прервана: Команда отменена";;
	esac
	echo "";;

"4") 	echo "Введите дату (yymmdd):"
	read day
	echo "Введите время (hh:mm:ss):"
	read time
	date +%Y%m%d -s "$date"
	date +%T -s "$time"
	echo "Введена дата:"
	data
	echo "";;

"5") 	echo "Вы вышли из программы"
	break
	echo "";;

*)	echo "Введено неверное значение, попробуйте ещё раз"
	echo "";;
esac
done
	
